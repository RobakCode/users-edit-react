## Users edit with React and JSON Server

In the project, you can run REST API with JSON Server and front application with React

## Start REST API

Go to the directory `db/` and run:

### `yarn start`

## Start React app

Go to the directory `app/` and run:

### `yarn start`